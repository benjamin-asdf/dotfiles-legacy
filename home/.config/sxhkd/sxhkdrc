# super + ctrl + k ; o ; b
#      bunel-open-idlegame-scene

super + ctrl + k ; o ; p
      bunel-open -a

super + ctrl + k ; l
      bunel-for-this-proj jump-to-last-thing-clicked

# super + ctrl + k ; s
#     bunel-select-in-scene

super + ctrl + k ; s ; b
      swap-default-browser

super + ctrl + k ; x
      xkill

super + ctrl + k ; j
      toggle-arrow-key-mode

super + ctrl + k ; i ; c
      idlegame-client

super + ctrl + k ; i ; s
      idlegame-server

super + ctrl + k ; i ; l
      idlegame-link

super + ctrl + k ; i ; L
      idlegame-link-extra.sh

super + ctrl + k ; i ; e
      bunel-for-this-proj open-scene "Assets/Scenes/LoadingScene.unity"


super + ctrl + k ; o ; o
      open-fav-prefab

super + ctrl + k ; o ; f
      set-fav-prefab

super + ctrl + k ; e
      emacs

super + ctrl + k ; r
      rider $IDLEGAMEDIR/IdleGame.sln



# f ; files
super + ctrl + k ; f ; p
      nautilus ~/Pictures/

super + ctrl + k ; f ; v
      nautilus ~/Videos/

# t ; trello
super + ctrl + k ; t
      trello-story-points
