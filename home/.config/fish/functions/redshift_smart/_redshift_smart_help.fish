function _redshift_smart_help --description 'display help for the redshift smart function'
    echo " Incremenentally change the color of the screen.

    Parameters:

    -i:
      Increase: Make the screen more orange compared to the current color.

    -d
      Decrease: Make the screen more blue compared to the current color.

    -t=TEMP
      Override the screen temperature to set TEMP.

    -x
      Reset the screen temperature to the default value.
    "
end
